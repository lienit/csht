<!DOCTYPE html>
<html lang="en" xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <title>绑定手机号</title>
    <th:block th:include="/include/bootstrap :: head"/>
    <link href="/css/dashboard.css" rel="stylesheet">
</head>
<body>
    <th:block th:replace="/include/bootstrap :: nav-userInfo"></th:block>
    <th:block th:replace="/include/bootstrap :: userinfo"></th:block>

    <div class="panel panel-default">
        <div class="panel-body" >
            <div class="col-sm-8 col-sm-offset-6 col-md-9 col-md-offset-1 main"  >
                <div class="row placeholders" >
                    <div class="col-md-5 col-md-offset-1" style="border: orange solid 1px;width: 900px;height: 900px ">
                        <div>
                            <label style="color: gray" th:if="${param.state.equals(0)}==false">
                                位置： 账号管理 > 安全中心 > 绑定手机号
                            </label>
                        </div>
                        <div style="margin-left: -100px;margin-top: 100px">
                            <label style="color: gray" th:if="${param.state.equals(0)}==false">
                                请输入要绑定的手机号：
                            </label>
                        </div>
                        <div style="margin-top: 100px;color: gray" th:if="${param.state.equals(0)}==false">

                            <form>
                                <div>
                                    <label  class="sr-only">绑定手机号：</label>
                                    <input type="text" name="mobile"  placeholder="请输入需要绑定的手机号" required autofocus />
                                </div>
                                <br/>
                                <div>
                                    <label  class="sr-only">密码验证 ：</label>
                                    <input type="password" name="psw"  placeholder="请输入密码，进行验证" required autofocus />
                                </div>
                                <button  id="btn1" type="submit">确认</button>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    </div>
</body>
</html>